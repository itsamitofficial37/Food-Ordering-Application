import SearchBar from "./SearchBar";
import RestaurantContainer from "./RestaurantContainer";
const Body = () => {
  return (
    <div className="body">
      <SearchBar />
      <RestaurantContainer />
    </div>
  );
};

export default Body;
